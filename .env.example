OPENAI_API_KEY=sk-your-openai-key
OPENAI_MODEL=gpt-5-nano
# Optional: point to a different base URL (for Azure/OpenAI-compatible endpoints)
# OPENAI_BASE_URL=https://api.openai.com/v1/chat/completions
# Set to true if your model only supports the default temperature (e.g., gpt-5-nano)
# OPENAI_FORCE_DEFAULT_TEMPERATURE=true
PORT=3000
